import * as session from './session/actions';
import * as participants from './participants/actions';
import * as observers from './observers/actions';

export default {
  session,
  participants,
  observers,
};
